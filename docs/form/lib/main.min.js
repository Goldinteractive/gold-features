(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("js-base"));
	else if(typeof define === 'function' && define.amd)
		define("feature-form", ["js-base"], factory);
	else if(typeof exports === 'object')
		exports["feature-form"] = factory(require("js-base"));
	else
		root["feature-form"] = factory(root["js-base"]);
})(typeof self !== 'undefined' ? self : this, function(__WEBPACK_EXTERNAL_MODULE_1__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _jsBase = __webpack_require__(1);

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var FEEDBACK_STATUS_SUCCESS = 0;
var FEEDBACK_STATUS_WARNING = 1;
var FEEDBACK_STATUS_ERROR = 2;

/**
 * Form feature class.
 */

var Form = function (_features$Feature) {
  _inherits(Form, _features$Feature);

  function Form() {
    _classCallCheck(this, Form);

    return _possibleConstructorReturn(this, (Form.__proto__ || Object.getPrototypeOf(Form)).apply(this, arguments));
  }

  _createClass(Form, [{
    key: 'init',
    value: function init() {
      this.isLoading = false;

      this.$form = this.$('form');
      this.$feedback = this.$('[data-feedback]');
      this.$progress = this.$('[data-progress]');
      this.token = null;

      this.$form.noValidate = true;

      this.addEventListener(this.$form, 'submit', this._submitListener());
      this.addEventListener(this.$form, 'reset', this._resetListener());

      this.obtainToken();
    }
  }, {
    key: 'obtainToken',
    value: function obtainToken() {
      var tokenEndpoint = this.node.dataset.tokenEndpoint || this.options.tokenEndpoint;
      if (tokenEndpoint !== null) {
        var tokenPromise = this.loadToken(tokenEndpoint);
        tokenPromise.then(this.getTokenJsonHandler());
      }
    }
  }, {
    key: 'getTokenJsonHandler',
    value: function getTokenJsonHandler() {
      var _this2 = this;

      return function (json) {
        _this2.setToken(json.data.token);
      };
    }
  }, {
    key: 'loadToken',
    value: function loadToken(tokenEndpoint) {
      return fetch(tokenEndpoint, {
        mode: 'cors',
        credentials: 'include'
      }).then(function (response) {
        return response.json();
      });
    }

    /**
     * Sets the token for the form and creates a hidden input
     * This method can only be called once - otherwise there might be multiple hidden inputs
     * @param {string} token 
     */

  }, {
    key: 'setToken',
    value: function setToken(token) {
      this.token = token;

      this.$form.appendChild(this.buildHiddenInput(this.options.tokenFieldName, token));
    }
  }, {
    key: 'buildHiddenInput',
    value: function buildHiddenInput(name, value) {
      var input = document.createElement('input');

      input.setAttribute('type', 'hidden');
      input.setAttribute('name', name);
      input.setAttribute('value', value);

      return input;
    }
  }, {
    key: 'fetch',
    value: function fetch(action, opts) {
      var _this3 = this;

      return new Promise(function (resolve, reject) {
        // using XMLHttpRequest due to fetch spec lacking support for request progression
        // see: https://fetch.spec.whatwg.org/#fetch-api
        var request = new XMLHttpRequest();
        _this3.currentRequest = request;

        _this3.triggerHub('form:beforeSend', opts, request);
        _this3.trigger('beforeSend', opts, request);

        request.withCredentials = true;
        request.open(opts.method || 'post', action);

        for (var h in opts.headers || {}) {
          if (opts.headers.hasOwnProperty(h)) {
            request.setRequestHeader(h, opts.headers[h]);
          }
        }

        request.onload = resolve;
        request.onerror = resolve;
        request.onabort = reject;
        request.ontimeout = reject;

        if (_this3.$progress && request.upload) {
          request.upload.onprogress = _this3._onProgress();
        }

        request.send(opts.body);
      }).then(function (e) {
        var request = e.target;
        try {
          var json = JSON.parse(request.responseText);
          return { json: json, request: request };
        } catch (jsonParseError) {
          // happens either on network issues or invalid response
          // anyway it's not possible to handle this exception
          throw jsonParseError;
        }
      });
    }
  }, {
    key: 'showFeedback',
    value: function showFeedback(msg) {
      var type = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : FEEDBACK_STATUS_SUCCESS;

      this.$feedback.innerHTML = msg;

      switch (type) {
        case FEEDBACK_STATUS_WARNING:
          this.$feedback.classList.add(this.options.feedbackClassWarning);
          break;
        case FEEDBACK_STATUS_ERROR:
          this.$feedback.classList.add(this.options.feedbackClassError);
          break;
        case FEEDBACK_STATUS_SUCCESS:
        default:
          this.$feedback.classList.add(this.options.feedbackClassSuccess);
      }
    }
  }, {
    key: 'resetFeedback',
    value: function resetFeedback() {
      this.$feedback.innerHTML = '';
      this.$feedback.classList.remove(this.options.feedbackClassWarning);
      this.$feedback.classList.remove(this.options.feedbackClassError);
      this.$feedback.classList.remove(this.options.feedbackClassSuccess);
    }
  }, {
    key: 'resetValidation',
    value: function resetValidation() {
      var _this4 = this;

      var $$formFields = this.$$('.' + this.options.formFieldClass);

      $$formFields.forEach(function ($field) {
        var $message = $field.querySelector(_this4.options.formFieldMessageElement + '.' + _this4.options.formFieldMessageClass);
        $field.classList.remove(_this4.options.formFieldClassError);

        if ($message) {
          $message.parentNode.removeChild($message);
        }
      });
    }
  }, {
    key: 'resetProgress',
    value: function resetProgress() {
      if (!this.$progress) return;
      this.$progress.classList.remove(this.options.progressClassShow);
      this.setProgress(0);
    }
  }, {
    key: 'setProgress',
    value: function setProgress(percentage) {
      if (!this.$progress || !this.options.showProgress) return;

      if (percentage === 0 || percentage === 1 && this.options.hideProgressOnEnd) {
        this.$progress.classList.remove(this.options.progressClassShow);
      } else {
        this.$progress.classList.add(this.options.progressClassShow);
      }

      percentage = percentage * 100;
      var percentageRounded = Math.ceil(percentage);

      var $loaded = this.$progress.querySelector('[data-loaded]');
      var $percentage = this.$progress.querySelector('[data-percentage]');

      $loaded.style.width = percentageRounded + '%';
      $percentage.textContent = percentageRounded + '%';
    }
  }, {
    key: 'displayFieldErrors',
    value: function displayFieldErrors(errors) {
      var _this5 = this;

      errors.forEach(function (error) {
        var fieldErrors = _this5.options.getFieldErrors.call(_this5, error);
        var $field = _this5.options.getErrorField.call(_this5, error);

        if ($field) {
          $field.classList.add(_this5.options.formFieldClassError);
          var $message = $field.querySelector(_this5.options.formFieldMessageElement + '.' + _this5.options.formFieldMessageClass);

          if (!$message) {
            $message = document.createElement(_this5.options.formFieldMessageElement);
            $message.classList.add(_this5.options.formFieldMessageClass);
            $field.appendChild($message);
          }

          $message.innerHTML = '';

          var $text = document.createElement('span');
          $text.textContent = fieldErrors[0];

          $message.appendChild($text);
        }
      });
    }
  }, {
    key: '_createFormData',
    value: function _createFormData() {
      var formData = new FormData(this.$form);
      return formData;
    }
  }, {
    key: '_onProgress',
    value: function _onProgress() {
      var _this6 = this;

      return function (e) {
        if (e.lengthComputable) {
          _this6.setProgress(e.loaded / e.total);
        }
      };
    }
  }, {
    key: '_resetListener',
    value: function _resetListener() {
      var _this7 = this;

      return function () {
        if (_this7.currentRequest) {
          _this7.currentRequest.abort();
        }

        _this7.resetProgress();
        _this7.resetFeedback();
        _this7.resetValidation();
      };
    }
  }, {
    key: '_submitListener',
    value: function _submitListener() {
      var _this8 = this;

      return function (e) {
        e.preventDefault();
        if (_this8.isLoading) return;

        _this8.isLoading = true;

        var formData = _this8._createFormData();
        var action = _this8.$form.getAttribute('action');
        var method = _this8.$form.getAttribute('method');

        _this8.resetProgress();
        _this8.resetFeedback();
        _this8.resetValidation();

        _this8.node.classList.add(_this8.options.classLoading);

        _this8.fetch(action, {
          body: formData,
          method: method
        }).then(function (response) {
          _this8.triggerHub('form:send', response);
          _this8.trigger('send', response);

          _this8.isLoading = false;
          _this8.node.classList.remove(_this8.options.classLoading);

          if (response.json.status >= 200 && response.json.status < 300) {
            _this8.triggerHub('form:sendSuccess', response);
            _this8.trigger('sendSuccess', response);

            _this8.showFeedback(response.json.message || _this8.options.defaultSuccessMessage);

            if (_this8.options.removeFormOnSuccess) {
              _this8.$form.parentNode.removeChild(_this8.$form);

              if (_this8.options.scroller) {
                window.setTimeout(function () {
                  _this8.options.scroller.toElement(_this8.$feedback || _this8.node);
                }, 0);
              }
            }
          } else {
            _this8.triggerHub('form:sendError', response);
            _this8.trigger('sendError', response);

            var errors = _this8.options.findFieldErrors(response.json);

            if (errors) {
              _this8.displayFieldErrors(errors);

              if (response.json.message) {
                _this8.showFeedback(response.json.message, FEEDBACK_STATUS_ERROR);
              }
            } else {
              _this8.showFeedback(response.json.message || _this8.options.defaultErrorMessage, FEEDBACK_STATUS_ERROR);
            }
          }
        }).catch(function (err) {
          _this8.isLoading = false;
          _this8.node.classList.remove(_this8.options.classLoading);

          // abort means user interrupted
          if (err.type === 'abort') {
            _this8.showFeedback(_this8.options.defaultAbortMessage, FEEDBACK_STATUS_WARNING);
            throw err;
          } else {
            _this8.showFeedback(_this8.options.defaultErrorMessage, FEEDBACK_STATUS_ERROR);
            throw err;
          }
        });
      };
    }
  }]);

  return Form;
}(_jsBase.features.Feature);

/**
 * Default feature options.
 */


Form.defaultOptions = {
  scroller: null,
  defaultSuccessMessage: 'Nachricht wurde erfolgreich versendet!',
  defaultErrorMessage: 'Beim Senden der Nachricht ist ein Fehler aufgetreten!',
  defaultAbortMessage: 'Sie haben das Senden abgebrochen.',
  classLoading: '-loading',
  removeFormOnSuccess: true,
  showProgress: true,
  hideProgressOnEnd: true,
  progressClassShow: '-show',
  formFieldClass: 'form-field',
  formFieldClassError: '-error',
  formFieldMessageElement: 'div',
  formFieldMessageClass: 'message',
  feedbackClassSuccess: '-success',
  feedbackClassWarning: '-warning',
  feedbackClassError: '-error',
  // priority: data-token-endpoint, options.tokenEndpoint, defaultOptions.tokenEndpoint
  tokenEndpoint: null,
  tokenFieldName: '_token',
  findFieldErrors: function findFieldErrors(json) {
    return json.errors;
  },
  getErrorField: function getErrorField(error) {
    var fieldName = error.id.replace('fields.', '');
    var $input = this.$form.querySelector('[name="fields[' + fieldName + ']"]');
    if (!$input) return null;
    return $input.closest('.' + this.options.formFieldClass);
  },
  getFieldErrors: function getFieldErrors(error) {
    return error.messages;
  }
};

exports.default = Form;

/***/ }),
/* 1 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_1__;

/***/ })
/******/ ]);
});
//# sourceMappingURL=main.min.js.map